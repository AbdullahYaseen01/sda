services:
  - type: web
    name: checkout-mvp
    env: python
    plan: starter
    buildCommand: pip install -r requirements_resnet.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 5000

